

        - for i in [1,2,3,4,5]
            div = i
        div 2 Test

|------------------------------------

-for i in var
    div = i
    - if i
        div 4 Test
    div 5 Test
    -for i in var
        div = i
        - if i
            div 4 Test
        div 5 Test


-for i in []


-for\
i\
        \
in \
[1,2,3,
        4,5]
    = i


- for i in []
    = i
- else
    -for i in []: -if i is not None: =i
    -else
        / This -else belongs to -if!!!
        / Controversially, but that's the way it's handled now (1)
        -if True
            Always
        -else
            Never
    -else
        / This -else should belong to -for (2)
        Oh my else
